/* ==========================================================================
   1. CONFIGURAÇÕES GERAIS, RESET E LAYOUT BASE
   ========================================================================== */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #f7f9fc;
    color: #333;
    line-height: 1.6;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

main {
    flex: 1;
}

h2, h3 {
    margin-bottom: 20px;
}

/* ==========================================================================
   2. COMPONENTES GLOBAIS (HEADER, FOOTER E NAV)
   ========================================================================== */
header {
    background-color: #1a1e27;
    color: white;
    padding: 1.5rem 0;
    border-bottom: 3px solid #007bff;
}

.header-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo { font-size: 1.8rem; font-weight: 800; }
.pro { color: #007bff; }

nav ul {
    display: flex;
    align-items: center;
    gap: 15px;
    list-style: none;
}

nav a {
    color: white;
    text-decoration: none;
    font-weight: 500;
    padding: 8px 12px;
    border-radius: 5px;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    gap: 8px;
}

nav a:hover, nav a.active {
    background-color: #007bff;
}

/* Colocar tudo na mesma linha */
.destaques-container {
    display: flex;
    justify-content: center; /* Centra o grupo todo na página */
    gap: 40px;               /* Espaço entre cada um dos três itens */
    margin-top: 30px;
    padding: 20px;
}

/* Estilo de cada item individual (Ícone + Texto) */
.destaque-item {
    display: flex;
    align-items: center;     /* Alinha o ícone verticalmente com o texto */
    gap: 10px;               /* Espaço entre o ícone e a palavra */
    color: white;            /* Cor do texto (ajusta se o fundo for claro) */
    font-weight: 500;
}

/* Pintar apenas os ícones de azul */
.destaque-item i {
    color: #007bff;          /* O azul oficial da tua TechShop Pro */
    font-size: 1.4rem;       /* Tamanho do ícone */
}

.badge {
    background-color: #ff4757;
    color: white;
    border-radius: 50%;
    padding: 2px 6px;
    font-size: 0.7rem;
    margin-left: -5px;
    margin-top: -10px;
}

footer {
    background-color: #1a1e27;
    color: white;
    text-align: center;
    padding: 20px;
}

/* ==========================================================================
   3. PÁGINA: HOME (INDEX)
   ========================================================================== */
.hero-section {
    background-image: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1542751371-adc38448a05e?q=80&w=2070&auto=format&fit=crop');
    background-size: cover;
    background-position: center;
    height: 83vh;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    text-align: center;
}

.hero-content h1 { font-size: 3rem; margin-bottom: 20px; }

/* ==========================================================================
   4. PÁGINA: PRODUTOS (CATÁLOGO E FILTROS)
   ========================================================================== */
.catalogo-layout {
    display: grid;
    grid-template-columns: 280px 1fr;
    gap: 30px;
    max-width: 1400px;
    margin: 40px auto;
    padding: 0 20px;
    align-items: start;
}

.coluna-esquerda {
    display: flex;
    flex-direction: column;
}

.titulo-pagina {
    font-size: 1.8rem;
    color: #1a1e27;
    margin-bottom: 20px;
    font-weight: bold;
}

.sidebar-filtros {
    background: white;
    padding: 25px;
    border-radius: 12px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    position: sticky;
    top: 20px;
    min-width: 280px;
    height: fit-content;
}

.filtro-grupo {
    margin-bottom: 20px;
}

.filtro-grupo label {
    display: block;
    font-weight: 600;
    margin-bottom: 8px;
}

.filtro-grupo input[type="text"], 
.filtro-grupo select {
    width: 100%;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 5px;
}

.produtos-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 20px;
}

/* ==========================================================================
   5. CARTÃO DE PRODUTO (USADO EM VÁRIAS PÁGINAS)
   ========================================================================== */
.produto-card {
    position: relative;
    background: white;
    border: 1px solid #eee;
    border-radius: 10px;
    padding: 20px;
    text-align: center;
    transition: transform 0.3s, box-shadow 0.3s;
    display: flex;
    flex-direction: column;
    height: 100%;
}

.produto-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 20px rgba(0,0,0,0.1);
}

.card-imagem {
    height: 180px;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 10px;
}

.produto-card img {
    width: 100%;
    height: 180px;
    object-fit: contain;
    margin-bottom: 15px;
}

.produto-titulo {
    font-size: 1.1rem;
    color: #333;
    margin-bottom: 15px;
    line-height: 1.4;
    min-height: 3.1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.produto-info {
    display: flex;
    flex-direction: column;
    flex: 1; 
}

.produto-info .preco-area {
    margin-top: auto !important; 
    margin-bottom: 15px;
}

.preco {
    font-size: 1.4rem;
    font-weight: bold;
    color: #007bff;
    margin-top: 0 !important;
    margin-bottom: 15px;
}

.btn-adicionar {
    background-color: #28a745;
    color: white;
    border: none;
    padding: 10px;
    border-radius: 5px;
    font-weight: bold;
    cursor: pointer;
    width: 100%;
    margin-top: auto;
}

.btn-adicionar:hover {
    background-color: #218838;
}

.btn-favorito {
    position: absolute;
    top: 15px;
    right: 15px;
    z-index: 10;
    background: rgba(255, 255, 255, 0.8);
    border: none;
    border-radius: 50%;
    width: 35px;
    height: 35px;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    color: #ff4757;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    transition: background 0.3s;
}

.btn-favorito:hover {
    background: rgba(255, 255, 255, 1);
    transform: scale(1.1);
}

.btn-favorito.favoritado {
    color: #ff4757;
    background: rgba(255, 71, 87, 0.1);
}

.btn-favorito:active {
    transform: scale(0.9);
}

/* ==========================================================================
   6. PÁGINAS: FAVORITOS E PROMOÇÕES (GRELHA CENTRADA)
   ========================================================================== */
.tag-desconto {
    position: absolute;
    top: 15px;          /* Distância do topo do cartão */
    left: 15px;         /* Distância da esquerda do cartão */
    background-color: #ff4757;
    color: white;
    padding: 5px 10px;
    border-radius: 5px;
    font-weight: bold;
    font-size: 0.85rem;
    z-index: 10;        /* Garante que fica por cima da imagem */
    box-shadow: 0 2px 5px rgba(0,0,0,0.2);
}

/* Quando o rato passa por cima de um coração já marcado na página de favoritos */
.pagina-favoritos .btn-favorito.favoritado:hover i {
    color: #ccc;
}

.cabecalho-favoritos {
    text-align: center;
    width: 100%;
    margin-top: 90px; 
    margin-bottom: 50px;
}

.cabecalho-favoritos h2 {
    font-size: 2rem !important; 
    font-weight: bold;
}

.pagina-promocoes {
    max-width: 1200px;
    margin: 40px auto;
    padding: 0 20px;
}

.cabecalho-promocoes {
    text-align: center;
    margin-bottom: 40px;
}

.cabecalho-promocoes h2 {
    font-size: 2rem;
    color: #1a1e27;
    margin-bottom: 10px;
}

.grelha-centrada {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: stretch;
    gap: 30px;
}

.grelha-centrada .produto-card {
    width: 280px;
}

/* ==========================================================================
   7. PÁGINA: CARRINHO
   ========================================================================== */
.container-carrinho {
    max-width: 1100px;
    margin: 40px auto;
    padding: 0 20px;
}

.carrinho-box {
    background: white;
    padding: 30px;
    border-radius: 16px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.04);
    border: 1px solid #eee;
}

.carrinho-box h2 {
    color: #1a1e27;
    margin-bottom: 25px;
    border-bottom: 2px solid #f0f4f8;
    padding-bottom: 15px;
}

.tabela-responsiva {
    overflow-x: auto;
}

#tabela-carrinho {
    width: 100%;
    border-collapse: collapse;
    margin-bottom: 30px;
}

#tabela-carrinho th {
    background-color: #f8f9fa;
    color: #555;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 0.85rem;
    padding: 15px;
    text-align: left;
    border-bottom: 2px solid #e1e5eb;
}

#tabela-carrinho td {
    padding: 15px;
    border-bottom: 1px solid #f0f0f0;
    vertical-align: middle;
}

.btn-remover {
    background: none;
    border: none;
    color: #ff4757;
    font-size: 1.2rem;
    cursor: pointer;
    transition: transform 0.2s;
}

.btn-remover:hover {
    transform: scale(1.1);
}

.carrinho-footer {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 20px;
    padding-top: 20px;
    border-top: 2px solid #f0f4f8;
}

.total-compra {
    font-size: 1.5rem;
    color: #1a1e27;
}

.total-compra span {
    font-weight: 800;
    color: #007bff;
    font-size: 1.8rem;
}

.btn-checkout {
    background-color: #10d43e;
    color: white;
    border: none;
    padding: 15px 30px;
    border-radius: 8px;
    font-weight: bold;
    font-size: 1.1rem;
    cursor: pointer;
    transition: background-color 0.3s;
}

.btn-checkout:hover {
    background-color: #218838;
}

/* ==========================================================================
   8. PÁGINA: REGISTO
   ========================================================================== */
.container-registo {
    max-width: 500px;
    margin: 60px auto;
    padding: 0 20px;
}

.form-box {
    background: white;
    padding: 40px;
    border-radius: 16px;
    box-shadow: 0 8px 20px rgba(0,0,0,0.04);
    border: 1px solid #eee;
}

.form-box h2 {
    color: #1a1e27;
    margin-bottom: 30px;
    text-align: center;
    border-bottom: 2px solid #f0f4f8;
    padding-bottom: 15px;
}

.input-group {
    margin-bottom: 20px;
    display: flex;
    flex-direction: column;
}

.input-group label {
    font-weight: 600;
    margin-bottom: 8px;
    color: #333;
}

.input-group input {
    padding: 12px 15px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 1rem;
    transition: all 0.3s;
}

.input-group input:focus {
    border-color: #007bff;
    outline: none;
    box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
}

.checkbox-group {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 20px;
}

.checkbox-group input {
    width: 18px;
    height: 18px;
    cursor: pointer;
}

.btn-registo {
    background-color: #007bff;
    color: white;
    border: none;
    padding: 15px;
    width: 100%;
    border-radius: 8px;
    font-weight: bold;
    font-size: 1.1rem;
    cursor: pointer;
    transition: background-color 0.3s;
}

.btn-registo:hover {
    background-color: #0056b3;
}

/* ==========================================================================
   9. PÁGINA: CONTACTOS
   ========================================================================== */
.container-contacto {
    max-width: 900px;
    margin: 60px auto;
    padding: 0 20px;
}

.contacto-wrapper {
    display: flex;
    gap: 60px;
    background-color: #ffffff; 
    padding: 50px;
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.05);
}

.contacto-info, .contacto-form {
    flex: 1;
}

.contacto-info h2, .contacto-form h2 {
    color: #333;
    font-size: 1.5rem; 
    white-space: nowrap; 
    margin-top: 0;
    margin-bottom: 25px;
    font-weight: bold;
}

.subtexto {
    color: #000000;
    margin-bottom: 40px;
    font-size: 0.95rem;
    line-height: 1.5;
}

.info-item {
    display: flex;
    align-items: flex-start;
    margin-bottom: 25px;
    gap: 15px;
}

.icone-azul {
    color: #0066cc;
    font-size: 1.3rem;
    margin-top: 2px;
    width: 30px;
    text-align: center;
}

.info-text h3 {
    color: #333 !important;
    font-size: 1.05rem;
    margin: 0 0 5px 0 !important;
    font-weight: bold;
    text-align: left;
}

.info-text p {
    color: #555;
    font-size: 0.95rem;
    margin: 0 !important;
    padding: 0;
    text-align: left;
}

.contacto-form .input-group label {
    font-size: 0.9rem;
    color: #000000;
    margin-bottom: 5px;
}

.contacto-form .input-group input, 
.contacto-form .input-group select, 
.contacto-form .input-group textarea {
    background: transparent;
    border: 1px solid #ccc;
    border-radius: 4px;
    padding: 10px;
    font-size: 0.9rem;
}

.btn-enviar {
    background-color: #0066cc;
    color: white;
    border: none;
    padding: 12px;
    width: 100%;
    border-radius: 4px;
    font-weight: bold;
    cursor: pointer;
    transition: 0.3s;
}

.btn-enviar:hover {
    background-color: #0052a3;
}

.info-item h3 {
    font-size: 1.1rem;
    color: #000000;
    margin-bottom: 5px;
    display: flex;
    align-items: center;
    gap: 10px;
}

.info-item p {
    color: #555;
    margin-left: 28px;
}

.contacto-form textarea {
    padding: 12px 15px;
    border: 1px solid #ddd;
    border-radius: 8px;
    font-size: 1rem;
    font-family: inherit;
    resize: vertical;
    transition: all 0.3s;
}

.contacto-form textarea:focus {
    border-color: #007bff;
    outline: none;
    box-shadow: 0 0 0 3px rgba(0, 123, 255, 0.1);
}

/* ==========================================================================
   10. RESPONSIVIDADE (MEDIA QUERIES)
   ========================================================================== */
@media (max-width: 992px) {
    .header-content { flex-direction: column; gap: 15px; }
    .catalogo-layout { grid-template-columns: 1fr; } 
    .contacto-wrapper { flex-direction: column; }
}

@media (max-width: 850px) {
    .catalogo-layout {
        grid-template-columns: 1fr;
    }
    .contacto-wrapper {
        flex-direction: column;
        padding: 30px;
        gap: 40px;
    }
}

@media (max-width: 600px) {
    .hero-content h1 { font-size: 2rem; }
    .produtos-grid { grid-template-columns: 1fr; }
}


